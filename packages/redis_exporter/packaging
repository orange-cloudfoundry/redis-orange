set -e
# Extract redis_exporter package
mkdir -p ${BOSH_INSTALL_TARGET}/bin ${BOSH_INSTALL_TARGET}/common
cp -a redis_exporter/utils.sh ${BOSH_INSTALL_TARGET}/common/.
tar xzf redis_exporter/redis_exporter-*.linux-amd64.tar.gz -C ${BOSH_INSTALL_TARGET}/bin
