#!/bin/bash
<% sentinel=link('redis_sentinel_conn')
  if sentinel.p('tls') %>
mkdir -p "<%= p('tls_keys_dir') %>";
openssl dhparam -out "<%= p('tls_keys_dir') %>/<%= p('tls_dh_params_file') %>" <%= p('tls_dh_length') %>;
<% end %>
exit 0;
